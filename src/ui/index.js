/**
 * UI
 * One dynamic header, a paragraph of main text and two buttons.
 */

import { init as initElements } from './elements'
export { setOnChoiceCallback } from './elements'
export { apply } from './apply'
export function init() {
  initElements()
}
