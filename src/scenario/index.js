/**
 * Scenario contract:
 * const scenario = {
 *  const write: () => { return [ headerText, mainText, button01Text, button02Text ] }
 *  const eval: (button id) => { return process to be applied to game scenario deck, can be undefined }
 * }
 */

export { callToAdventure } from './callToAdventure'
export { example } from './example'